version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
    - 5432:5432
    environment:
      POSTGRES_USER: bo
      POSTGRES_DB: badoom_dev
      POSTGRES_PASSWORD: badoom
